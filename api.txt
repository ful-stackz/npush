Current <package> version is 1.2.3

Pick next version:
> 1.2.4 (Patch update)
  1.3.0 (Minor update)
  2.0.0 (Major update)

Pick release flag:
> Release (1.2.4)
  Preview (1.2.4-preview)
  Beta (1.2.4-beta)
  Alpha (1.2.4-alpha)


// command usage
npush

  OPTIONAL ARGUMENTS:

  --update, -u <major|minor|patch>          Sets the update type.
  --stage, -s <alpha|beta|preview|release>  Sets the release stage.
  --dry                                     Runs the application in dry mode. If enabled then nothing will be published.
  --log <info|debug>                        Sets the log level. Defaults to info.
